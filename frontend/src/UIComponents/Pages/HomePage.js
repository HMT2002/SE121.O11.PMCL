import React from "react";

const HomePage = (props) => {
    return (
        <React.Fragment>
            <div className="page">Home Page</div>
        </React.Fragment>
    );
}

export default HomePage;